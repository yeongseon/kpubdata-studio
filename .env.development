# 데모/개발 환경 기본 설정
#
# `npm run dev` 실행 시 자동 로드됨 (Vite 기본 동작)
# 실 Builder 연동 시 VITE_USE_REAL_BUILDER=true로 변경

# Builder 연결 모드: false=데모(mock), true=실 Builder API 연동
VITE_USE_REAL_BUILDER=false

# Builder API URL (VITE_USE_REAL_BUILDER=true일 때 사용)
VITE_BUILDER_API_URL=http://localhost:8000
